{
  "name": "rinter",
  "version": "2.0.0-11",
  "description": "Minimalist state container based on reactive extensions",
  "main": "lib/index.js",
  "module": "lib-esm/index.js",
  "repository": "https://github.com/dfernandez79/rinter.git",
  "author": "Diego Fernandez <diegof79@gmail.com>",
  "license": "MIT",
  "sideEffects": false,
  "keywords": [
    "state",
    "predictable",
    "reducer",
    "rxjs",
    "flux",
    "elm"
  ],
  "peerDependencies": {
    "rxjs": "6.x"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.6",
    "@babel/preset-env": "^7.1.6",
    "@babel/register": "^7.0.0",
    "ava": "^1.2.1",
    "cpy-cli": "^2.0.0",
    "eslint": "^5.9.0",
    "np": "^4.0.2",
    "npm-run-all": "^4.1.3",
    "nyc": "^13.1.0",
    "rimraf": "^2.6.2",
    "rxjs": "^6.3.3"
  },
  "scripts": {
    "build": "npm-run-all build:lib build:esm",
    "build:lib": "babel src -d lib",
    "build:esm": "cpy '**/*.js' '!**/*.test.js' --cwd=src ../lib-esm",
    "test": "ava",
    "coverage": "npm-run-all build:lib coverage:run",
    "coverage:run": "nyc --reporter=html ava",
    "clean": "rimraf lib lib-esm *.tgz coverage .nyc_output *.log",
    "lint": "eslint src",
    "release": "npm-run-all clean lint build release:np",
    "release:np": "np"
  },
  "ava": {
    "files": [
      "src/**/*.test.js"
    ],
    "require": [
      "@babel/register"
    ],
    "babel": {
      "testOptions": {
        "babelrc": false
      }
    }
  }
}
